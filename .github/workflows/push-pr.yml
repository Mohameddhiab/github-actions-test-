on:
    push:
        branches:
            - main 
            - "release/**"
        paths:
            -"src/**"
    pull_request:
        branches:
            - main
        paths:
            - "src/**"
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4.2.2
            - name: run tests
              run: npm test 